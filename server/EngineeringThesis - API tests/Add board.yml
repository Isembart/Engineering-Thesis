info:
  name: Add board
  type: http
  seq: 4

http:
  method: POST
  url: "{{host}}/add-board"
  body:
    type: json
    data: |-
      {
        "mac_address": 1234
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
