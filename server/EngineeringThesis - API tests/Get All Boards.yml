info:
  name: Get All Boards
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/get-all-boards"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
