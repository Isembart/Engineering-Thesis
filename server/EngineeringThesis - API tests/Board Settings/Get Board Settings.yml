info:
  name: Get Board Settings
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/board-settings?mac_address=1234"
  params:
    - name: mac_address
      value: "1234"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
